<h1> <%= @cocktail.name %> </h1>

<h2>Ingredients</h2>
<% @cocktail.doses.each do |d| %>
<p><%= d.description %> of <%= d.ingredient.name %><%= link_to "Delete", dose_path(d), :method => :delete %>
<%= link_to "Edit", edit_dose_path(d.id) %>
</p>
<% end %>

<%= link_to "Add ingredients", new_cocktail_dose_path(@cocktail)  %>